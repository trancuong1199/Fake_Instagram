import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    listBlogs: {
        flexDirection: 'row'
    },
    blogImages: {
        width: 130,
        height: 120
    },

});

export default styles;
